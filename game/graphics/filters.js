'use strict';

/*
* Filters for images
* each filters simply take an image in argument and return a new image
*/

let filters = (function() {
    
    let blackWhite = function(img) {
        return img;
    }
    
    
    return {
        blackWhite,
    }
})();


